export default function DietaryBadge({ label }) {
  return <span className="bg-secondary-fixed text-on-secondary-container text-[11px] font-body uppercase tracking-wider px-3 py-1 rounded-full">{label}</span>;
}
